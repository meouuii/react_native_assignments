
├── android/                     # native Android project (auto-generated)
├── ios/                         # native iOS project (auto-generated)
├── src/
│   ├── api/
│   │   └── api.js               # Axios instance (baseURL + timeout)
│   ├── context/
│   │   └── ThemeContext.tsx     # Context API — light/dark theme
│   └── components/
│       ├── Button.tsx           # reusable button (props, defaults, children)
│       ├── Card.tsx             # reusable card (children prop)
│       ├── Input.tsx            # reusable labeled text input
│       └── Loading.tsx          # reusable loading indicator
├── App.tsx                      # root component — navigation, tabs, screens
├── index.js                     # app entry point
├── app.json
├── babel.config.js
├── metro.config.js
├── tsconfig.json
├── package.json
└── package-lock.json
